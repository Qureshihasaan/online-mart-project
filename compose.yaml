# version: '3.8'
name: template01
services:
  product_services:
    build:
      context: ./product_services
      dockerfile: Dockerfile
    volumes:
      - ./product_services:/code # Sync local development directory with the container
    #   - DATABASE_URL=postgresql://hasaanqureshi:my_password1122@product_db:5432/product_services_db
    #   - BOOTSTRAP_SERVER=broker:19092
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - PINECONE_API_KEY=${PINECONE_API_KEY}
      - PINECONE_INDEX_NAME=${PINECONE_INDEX_NAME}
    depends_on:
      - broker
        # condition: service_healthy
        # product_db:
        #   condition: service_healthy
        # broker:
        #   condition: service_started
    ports:
      - "8000:8000" # Expose container port 8000 to host port 8000
    restart: on-failure
  # product_db:
  #   image: postgres:15  # Use the official PostgreSQL image
  #   restart: always
  #   container_name: product_db_container
  #   environment:
  #       # - POSTGRES_USER=hasaanqureshi
  #       # - POSTGRES_PASSWORD=my_password1122
  #       # - POSTGRES_DB=product_services_db
  #       - POSTGRES_USER=${POSTGRES_USER_PRODUCT}
  #       - POSTGRES_PASSWORD=${POSTGRES_PASSWORD_PRODUCT}
  #       - POSTGRES_DB=${POSTGRES_DB_PRODUCT}
  #   ports:
  #       - '5432:5432'
  #   volumes:
  #       - product_db:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER_PRODUCT} -d ${POSTGRES_DB_PRODUCT}"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

  inventory_services:
    build:
      context: ./inventory_services
      dockerfile: Dockerfile
    volumes:
      - ./inventory_services:/code # Sync local development directory with the container
    depends_on:
      - broker
        # condition: service_healthy
        # inventory_db:
        #   condition: service_healthy
        # broker:
        #   condition: service_started
    ports:
      - "8001:8000" # Expose container port 8000 to host port 8000
    restart: on-failure

  # inventory_db:
  #   image: postgres:15  # Use the official PostgreSQL image
  #   restart: always
  #   container_name: inventory_db_container
  #   environment:
  #       - POSTGRES_USER=${POSTGRES_USER_INVENTORY}
  #       - POSTGRES_PASSWORD=${POSTGRES_PASSWORD_INVENTORY}
  #       - POSTGRES_DB=${POSTGRES_DB_INVENTORY}
  #   ports:
  #       - '5433:5432'
  #   volumes:
  #       - inventory_db:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER_INVENTORY} -d ${POSTGRES_DB_INVENTORY}"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

  user_services:
    build:
      context: ./user_services
      dockerfile: Dockerfile
    volumes:
      - ./user_services:/code # Sync local development directory with the container
    depends_on:
      - broker
        # condition: service_healthy
        # user_db:
        #   condition: service_healthy
        # broker:
        #   condition: service_started
    ports:
      - "8002:8000" # Expose container port 8000 to host port 8000
    restart: on-failure

  # user_db:
  #   image: postgres:15  # Use the official PostgreSQL image
  #   restart: always
  #   container_name: user_services_db_container
  #   environment:
  #       - POSTGRES_USER=${POSTGRES_USER_USER}
  #       - POSTGRES_PASSWORD=${POSTGRES_PASSWORD_USER}
  #       - POSTGRES_DB=${POSTGRES_DB_USER}
  #   ports:
  #       - '5434:5432'
  #   volumes:
  #       - user_db:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER_USER} -d ${POSTGRES_DB_USER}"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

  order_services:
    build:
      context: ./order_services
      dockerfile: Dockerfile
    volumes:
      - ./order_services:/code # Sync local development directory with the container
    depends_on:
      - broker
        # condition: service_healthy

        # order_db:
        #   condition: service_healthy
        # broker:
        #   condition: service_started
    ports:
      - "8003:8000" # Expose container port 8000 to host port 8000
    restart: on-failure

  # order_db:
  #   image: postgres:15  # Use the official PostgreSQL image
  #   restart: always
  #   container_name: order_db_container
  #   environment:
  #       - POSTGRES_USER=${POSTGRES_USER_ORDER}
  #       - POSTGRES_PASSWORD=${POSTGRES_PASSWORD_ORDER}
  #       - POSTGRES_DB=${POSTGRES_DB_ORDER}
  #   ports:
  #       - '5435:5432'
  #   volumes:
  #       - order_db:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER_ORDER} -d ${POSTGRES_DB_ORDER}"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

  notification_services:
    build:
      context: ./notification_services
      dockerfile: Dockerfile
    volumes:
      - ./notification_services:/code
    depends_on:
      # - notification_db
      - broker # condition: service_healthy
    ports:
      - "8004:8000"
    restart: on-failure

  # notification_db:
  #   image: postgres:latest
  #   restart : always
  #   container_name: notification_db_container
  #   environment:
  #     - POSTGRES_USER=hasaanqureshi
  #     - POSTGRES_PASSWORD=Abcd9196
  #     - POSTGRES_DB=notification_service_db
  #   ports:
  #     - "5438:5432"
  #   volumes:
  #     - notification_db:/var/lib/postgresql/data

  payment_services:
    build:
      context: ./payment_services
      dockerfile: Dockerfile
    volumes:
      - ./payment_services:/code
    depends_on:
      - broker # condition: service_healthy
      # payment_db:
      #   condition: service_healthy
      # broker:
      #   condition: service_started
    ports:
      - "8005:8000"
    restart: on-failure

  # payment_db:
  #   image: postgres:15
  #   restart: always
  #   container_name: payment_db_container
  #   environment:
  #     - POSTGRES_USER=${POSTGRES_USER_PAYMENT}
  #     - POSTGRES_PASSWORD=${POSTGRES_PASSWORD_PAYMENT}
  #     - POSTGRES_DB=${POSTGRES_DB_PAYMENT}
  #   ports:
  #     - "5436:5432"
  #   volumes:
  #     - payment_db:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER_PAYMENT} -d ${POSTGRES_DB_PAYMENT}"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

  # pgadmin:
  #   image: dpage/pgadmin4:latest
  #   environment:
  #     PGADMIN_DEFAULT_EMAIL= hasaanqureshi195@gmail.com
  #     PGADMIN_DEFAULT_PASSWORD= Abcd9197
  #   ports:
  #     -"5050:80"
  #   depends_on:
  #     - product_db
  #     - inventory_db
  #     - user_services_db
  #     - order_services_db
  # pgadmin:
  #   image: dpage/pgadmin4:latest
  #   container_name: pgadmin
  #   environment:
  #     PGADMIN_DEFAULT_EMAIL: hasaanqureshi150@gmail.com
  #     PGADMIN_DEFAULT_PASSWORD: Abcd9197
  #   ports:
  #     - "8080:80"
  #   depends_on:
  #     - product_db
  #     - inventory_db
  #     - user_services_db
  #     - order_services_db

  broker:
    image: apache/kafka:3.7.0
    hostname: broker
    container_name: broker
    ports:
      - "9093:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: "CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT"
      KAFKA_ADVERTISED_LISTENERS: "PLAINTEXT_HOST://localhost:9092,PLAINTEXT://broker:19092"
      KAFKA_PROCESS_ROLES: "broker,controller"
      KAFKA_CONTROLLER_QUORUM_VOTERS: "1@broker:29093"
      KAFKA_LISTENERS: "CONTROLLER://:29093,PLAINTEXT_HOST://:9092,PLAINTEXT://:19092"
      KAFKA_INTER_BROKER_LISTENER_NAME: "PLAINTEXT"
      KAFKA_CONTROLLER_LISTENER_NAMES: "CONTROLLER"
      CLUSTER_ID: "4L6g3nShT-eMCtK--X86sw"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_LOG_DIRS: "/tmp/kraft-combined-logs"
    # healthcheck:
    #   test:
    #     [
    #       "CMD-SHELL",
    #       "kafka-broker-api-versions --bootstrap-server localhost:9092 || exit 1",
    #     ]
    #   interval: 10s
    #   timeout: 10s
    #   retries: 5
    #   start_period: 40s

  kafka-ui:
    image: provectuslabs/kafka-ui
    container_name: kafka-ui
    ports:
      - "8081:8080"
    environment:
      KAFKA_CLUSTERS_0_NAME: "Local Kafka Cluster"
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: "broker:19092"
      DYNAMIC_CONFIG_ENABLED: "true"
    depends_on:
      - broker # condition: service_healthy
    restart: on-failure

  #### Kong Service

  # kong-database:
  #   image: postgres:13
  #   container_name: kong-database
  #   environment:
  #     POSTGRES_USER: kong
  #     POSTGRES_DB: kong
  #     POSTGRES_PASSWORD: kongpass
  #   ports:
  #     - "5437:5432"
  #   volumes:
  #     - kong_data:/var/lib/postgresql/data

  # kong-migrations:
  #   image: kong:3.6
  #   command: kong migrations bootstrap
  #   depends_on:
  #     - kong-database
  #   environment:
  #     KONG_DATABASE: postgres
  #     KONG_PG_HOST: kong-database
  #     KONG_PG_USER: kong
  #     KONG_PG_PASSWORD: kongpass
  #     KONG_CASSANDRA_CONTACT_POINTS: kong-database

  # kong:
  #   image: kong:3.6
  #   container_name: kong
  #   restart: always
  #   volumes:
  #     - ./kong.yml:/usr/local/kong/declarative/kong.yml
  #   depends_on:
  #     - kong-database
  #   environment:
  #     KONG_DATABASE: "off"
  #     KONG_DECLARATIVE_CONFIG: /usr/local/kong/declarative/kong.yml
  #     KONG_PROXY_ACCESS_LOG: /dev/stdout
  #     KONG_ADMIN_ACCESS_LOG: /dev/stdout
  #     KONG_PROXY_ERROR_LOG: /dev/stderr
  #     KONG_ADMIN_ERROR_LOG: /dev/stderr
  #     KONG_ADMIN_LISTEN: "0.0.0.0:8009"
  #     KONG_PROXY_LISTEN: "0.0.0.0:8008"
  #   ports:
  #     - "8009:8000"  # Kong Proxy (client-facing)
  #     - "8008:8008"  # Kong Admin API
  #   networks:
  #     - default

  ai_chatbot:
    build:
      context: ./ai_services/ai_chatbot
      dockerfile: Dockerfile
    volumes:
      - ./ai_services/ai_chatbot:/code
    ports:
      - "8006:8000"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - PINECONE_API_KEY=${PINECONE_API_KEY}
    restart: on-failure

  ai_design_visualization:
    build:
      context: ./ai_services/ai_design_generation_visualization
      dockerfile: Dockerfile
    volumes:
      - ./ai_services/ai_design_generation_visualization:/code
    ports:
      - "8007:8000"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - OPENROUTER_BASE_URL=${OPENROUTER_BASE_URL}
      - OPENROUTER_MODEL=${OPENROUTER_MODEL}
      - FLUX_IMAGE_MODEL=${FLUX_IMAGE_MODEL}
    restart: on-failure

# volumes:
#   product_db:
#     driver: local
#   inventory_db:
#     driver: local
#   user_db:
#     driver: local
#   order_db:
#     driver: local
#   # notification_db:
#   #   driver: local
#   payment_db:
#     driver: local
# kong_data:
#   driver: local

networks:
  default:
    driver: bridge
